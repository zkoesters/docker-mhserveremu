name: Docker Image CI
on:
  push:
    branches: [ "main" ]
    paths:
      - "Dockerfile"
      - "Dockerfile.alpine"
      - "docker-entrypoint.sh"
      - "start-server"
      - "**/Config.ini.template"
      - "Makefile"
  workflow_dispatch:
jobs:
  build:
    uses: ./.github/workflows/docker-build-push.yml
    permissions:
      actions: write
      contents: read
      packages: write
      attestations: write
      id-token: write
    with:
      matrix: >-
        [
          {"repository": "https://github.com/Crypto137/MHServerEmu.git", "ref": "1.0.0", "commit": "", "version_dir": "1.0.0", "tag": "1.0.0",        "dockerfile": "Dockerfile"},
          {"repository": "https://github.com/Crypto137/MHServerEmu.git", "ref": "1.0.0", "commit": "", "version_dir": "1.0.0", "tag": "1.0.0-alpine", "dockerfile": "Dockerfile.alpine"},
          {"repository": "https://github.com/Crypto137/MHServerEmu.git", "ref": "1.0.1", "commit": "", "version_dir": "1.0.1", "tag": "1.0.1",        "dockerfile": "Dockerfile"},
          {"repository": "https://github.com/Crypto137/MHServerEmu.git", "ref": "1.0.1", "commit": "", "version_dir": "1.0.1", "tag": "1.0.1-alpine", "dockerfile": "Dockerfile.alpine"}
        ]
    secrets:
      RCLONE_CONFIG: ${{ secrets.RCLONE_CONFIG }}
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
