apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: mhserveremu
  namespace: mhserveremu
spec:
  virtualhost:
    fqdn: mhserveremu.localdev
  routes:
    - conditions:
        - prefix: /
      services:
        - name: mhserveremu
          port: 8080
