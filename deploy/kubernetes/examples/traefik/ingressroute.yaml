apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: mhserveremu
  namespace: mhserveremu
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`mhserveremu.localdev`) && PathPrefix(`/`)
      kind: Rule
      services:
        - name: mhserveremu
          port: 8080
