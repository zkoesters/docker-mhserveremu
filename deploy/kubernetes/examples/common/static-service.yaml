apiVersion: v1
kind: Service
metadata:
  name: mhserveremu-static
  namespace: mhserveremu
  labels:
    app: mhserveremu-static
spec:
  selector:
    app: mhserveremu-static
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
